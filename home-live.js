function liveSafe(value=''){return String(value).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');}
function standingsMarkup(items=[]){if(!items.length)return '<div class="card-pad"><strong>Standings are temporarily unavailable.</strong><p>The encyclopedia is still open.</p></div>';return `<div class="standing-row head"><span>#</span><span>Team</span><span>W</span><span>L</span><span>PCT</span></div>${items.map((item,index)=>`<div class="standing-row"><span class="rank">${item.playoff_seed||index+1}</span><span class="team-name">${liveSafe(item.team?.full_name||'Unknown team')}</span><strong>${item.wins??'—'}</strong><strong>${item.losses??'—'}</strong><span>${Number.isFinite(Number(item.win_percentage))?Number(item.win_percentage).toFixed(3):'—'}</span></div>`).join('')}`;}
function resultsMarkup(items=[]){if(!items.length)return '<div class="home-result"><strong>No recent finals returned.</strong><span>Check back after the next completed games.</span></div>';return items.slice(0,5).map(game=>`<article class="home-result"><span>${liveSafe(game.date||'Final')}</span><strong>${liveSafe(game.awayTeam||'TBD')} ${game.awayScore??'—'}–${game.homeScore??'—'} ${liveSafe(game.homeTeam||'TBD')}</strong><span>Final</span></article>`).join('');}
async function loadHomeLive(){const table=document.getElementById('homeStandings'),results=document.getElementById('homeResults'),status=document.getElementById('homeLiveStatus');if(!table||!results)return;try{const response=await fetch('/api/stats?season=2026',{headers:{Accept:'application/json'}});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||'Live data unavailable');table.innerHTML=standingsMarkup(payload.standings);results.innerHTML=resultsMarkup(payload.recentResults);status.textContent=payload.fullSeasonAccess?`Live via independent feed • updated ${new Date(payload.updatedAt).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}`:'Independent feed connected';}catch(error){table.innerHTML='<div class="card-pad"><strong>Live standings could not load.</strong><p>Try again shortly.</p></div>';results.innerHTML='<div class="home-result"><strong>Recent results unavailable.</strong></div>';status.textContent='Live data temporarily unavailable';}}
loadHomeLive();