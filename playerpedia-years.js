(()=>{let years=new Map();const key=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]/g,'');function decorate(){document.querySelectorAll('#playerGrid .player-card').forEach(card=>{if(card.querySelector('.player-card-years'))return;const name=card.querySelector('strong')?.textContent?.trim();if(!name)return;const item=years.get(key(name));const badge=document.createElement('small');badge.className='player-card-years';badge.textContent=item?.label||'WNBA · current';card.querySelector('.player-card-copy')?.appendChild(badge);});}fetch('/api/player-years?season=2026',{headers:{Accept:'application/json'}}).then(r=>r.json()).then(p=>{years=new Map((p.players||[]).map(x=>[key(x.name),x]));decorate();}).catch(()=>decorate());const host=document.getElementById('playerGrid');if(host)new MutationObserver(decorate).observe(host,{childList:true,subtree:true});setTimeout(decorate,800);})();
