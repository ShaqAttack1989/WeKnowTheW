(function(){const esc=(value='')=>String(value).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[ch]));const slug=new URLSearchParams(location.search).get('team')||'';const mascots=typeof CURRENT_MASCOTS!=='undefined'?CURRENT_MASCOTS.filter(item=>item.slug===slug):[];const retired=typeof RETIRED_MASCOTS!=='undefined'?RETIRED_MASCOTS.filter(item=>item.slug===slug):[];const vacancy=typeof MASCOT_VACANCIES!=='undefined'?MASCOT_VACANCIES.find(item=>item.slug===slug):null;const fan=window.TEAM_FANBASE_IDENTITIES?.[slug]||{name:'Team fans',kind:'Fan community',source:'/around-the-w.html'};const teamMeta=typeof MASCOT_TEAMS!=='undefined'?MASCOT_TEAMS[slug]:null;const colors=teamMeta?`--team:${teamMeta.primary};--team2:${teamMeta.secondary}`:'--team:#5b2bbf;--team2:#d7ff42';function initials(name=''){return String(name).split(/\s+/).filter(Boolean).slice(0,2).map(part=>part[0]).join('').toUpperCase();}function mascotCard(item){const intro=item.introduced||item.debut||'';return `<article class="team-culture-placecard mascot" style="${colors}" data-team-mascot="${esc(item.id)}"><div class="team-culture-photo" data-team-mascot-photo="${esc(item.id)}"><span>${esc(initials(item.name))}</span></div><div><span class="panel-label">MASCOT</span><h3>${esc(item.displayName||item.name)}</h3><div class="identity-meta"><span>${esc(item.species||'Mascot')}</span>${intro?`<span>Introduced ${esc(intro)}</span>`:''}</div><p>${esc(item.bio||'The team’s game-night mascot and community ambassador.')}</p><a href="/mascots.html?team=${encodeURIComponent(slug)}">Open mascot bio →</a></div></article>`;}function noMascotCard(){const legacy=retired[0],note=vacancy?.note||'No current mascot has been officially announced.';return `<article class="team-culture-placecard mascot team-culture-empty" style="${colors}"><div class="team-culture-photo"><span>?</span></div><div><span class="panel-label">MASCOT</span><h3>Not currently announced</h3><p>${esc(note)}</p>${legacy?`<p><strong>Legacy mascot:</strong> ${esc(legacy.displayName||legacy.name)}</p>`:''}<a href="/mascots.html#mascot-watch">Open Mascot Watch →</a></div></article>`;}function fanCard(){const source=fan.source||'/around-the-w.html',external=/^https?:\/\//.test(source);return `<article class="team-culture-placecard fanbase" style="${colors}"><span class="panel-label">FANBASE</span><h3>${esc(fan.name)}</h3><p>${esc(fan.kind||'Fan community')}</p><div class="identity-meta"><span>${esc(teamMeta?.team||'WNBA team')}</span><span>Home-court identity</span></div><a class="team-culture-source" href="${esc(source)}"${external?' target="_blank" rel="noopener"':''}>Fanbase source ${external?'↗':'→'}</a></article>`;}async function hydratePhotos(root){for(const host of root.querySelectorAll('[data-team-mascot-photo]')){const item=mascots.find(entry=>entry.id===host.dataset.teamMascotPhoto);if(!item)continue;let image=item.image||'';if(!image&&item.source){try{const response=await fetch(`/api/culture-image?url=${encodeURIComponent(item.source)}`);const payload=await response.json();if(payload?.found)image=payload.image||'';}catch{}}if(!image)continue;const img=document.createElement('img');img.src=image;img.alt=`${item.displayName||item.name}, ${teamMeta?.team||'WNBA'} mascot`;img.loading='lazy';img.decoding='async';img.addEventListener('error',()=>img.remove(),{once:true});host.querySelector('span')?.remove();host.prepend(img);}}function render(){const peopleGrid=document.getElementById('teamPeopleGrid');if(!peopleGrid||document.getElementById('teamCulturePlacecards'))return false;const host=document.createElement('div');host.id='teamCulturePlacecards';host.className='team-culture-placecards';host.innerHTML=mascots.length>1?`<div class="double-mascot">${mascots.map(mascotCard).join('')}</div>${fanCard()}`:`${mascots.length?mascotCard(mascots[0]):noMascotCard()}${fanCard()}`;peopleGrid.parentNode.insertBefore(host,peopleGrid);hydratePhotos(host);return true;}if(!render()){const observer=new MutationObserver(()=>{if(render())observer.disconnect();});observer.observe(document.body,{childList:true,subtree:true});}if(!document.querySelector('script[data-team-rivalry]')){const script=document.createElement('script');script.src='/team-rivalry.js?v=20260823-v1';script.dataset.teamRivalry='true';document.body.appendChild(script);}if(!document.querySelector('script[data-legacy-team-assets]')){const legacy=document.createElement('script');legacy.src='/legacy-team-assets.js?v=20260823-v1';legacy.dataset.legacyTeamAssets='true';document.body.appendChild(legacy);}})();
