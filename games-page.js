let gamesPayload=null,competitionPayload=null,gamesMode='live',gamesTeam='all',gamesCompetition='season',gamesRefreshActive=false;
function compGames(){if(gamesCompetition==='cup')return [...(competitionPayload?.cup?.poolGames||[]),...(competitionPayload?.cup?.championshipGames||[])];if(gamesCompetition==='playoffs')return competitionPayload?.playoffs?.games||[];return null;}
function temporal(items=[]){const now=Date.now();return items.filter(g=>{const t=Date.parse(g.startTimeUtc||`${g.date}T12:00:00Z`);if(gamesMode==='past')return g.completed===true;if(gamesMode==='live')return g.state==='in';return !g.completed&&g.state!=='in'&&Number.isFinite(t)&&t>=now;});}
function renderGames(){const list=document.getElementById('gamesList'),title=document.getElementById('gamesTitle'),past=document.getElementById('gamesPastToggle'),upcoming=document.getElementById('gamesUpcomingToggle'),live=document.getElementById('gamesLiveToggle');if(!list||!gamesPayload||!window.WGameCards)return;let source;if(gamesCompetition==='season'){source=gamesMode==='live'?gamesPayload.liveGames||[]:gamesMode==='upcoming'?gamesPayload.upcomingGames||[]:(gamesPayload.pastGames||gamesPayload.recentResults||[]).filter(WGameCards.finalGame);}else source=temporal(compGames());const items=WGameCards.filter(source,gamesTeam);const label=gamesCompetition==='cup'?'Commissioner’s Cup':gamesCompetition==='playoffs'?'Playoffs':'Regular Season';title.textContent=`${label} · ${gamesMode==='live'?'Live':gamesMode==='upcoming'?'Upcoming':'Past'} Games`;live?.classList.toggle('active',gamesMode==='live');past?.classList.toggle('active',gamesMode==='past');upcoming?.classList.toggle('active',gamesMode==='upcoming');live?.setAttribute('aria-pressed',String(gamesMode==='live'));past?.setAttribute('aria-pressed',String(gamesMode==='past'));upcoming?.setAttribute('aria-pressed',String(gamesMode==='upcoming'));if(!items.length&&gamesCompetition==='playoffs'&&!competitionPayload?.playoffs?.started){list.innerHTML='<div class="schedule-empty"><strong>Playoffs begin Sept. 27.</strong><p>The postseason schedule and results will populate here as soon as they are published.</p></div>';return;}list.innerHTML=WGameCards.render(items,gamesMode,{limit:36,standings:gamesPayload.standings||[]});}
document.getElementById('gamesLiveToggle')?.addEventListener('click',()=>{gamesMode='live';renderGames();});document.getElementById('gamesUpcomingToggle')?.addEventListener('click',()=>{gamesMode='upcoming';renderGames();});document.getElementById('gamesPastToggle')?.addEventListener('click',()=>{gamesMode='past';renderGames();});document.getElementById('gamesTeamFilter')?.addEventListener('change',e=>{gamesTeam=e.target.value||'all';renderGames();});document.querySelectorAll('[data-games-competition]').forEach(button=>button.addEventListener('click',()=>{gamesCompetition=button.dataset.gamesCompetition;document.querySelectorAll('[data-games-competition]').forEach(b=>{const on=b===button;b.classList.toggle('active',on);b.setAttribute('aria-pressed',String(on));});const note=document.getElementById('gamesCompetitionNote');if(gamesCompetition==='cup'){gamesMode='past';if(note)note.textContent='Pool play June 1–17, championship June 30';}else if(gamesCompetition==='playoffs'){gamesMode=competitionPayload?.playoffs?.started?'live':'upcoming';if(note)note.textContent='Playoffs begin Sept. 27';}else if(note)note.textContent='Regular-season schedule';renderGames();}));
async function loadGames(initial=false){if(gamesRefreshActive)return;gamesRefreshActive=true;const status=document.getElementById('gamesStatus');try{const [stats,comp]=await Promise.all([fetch('/api/stats?season=2026',{headers:{Accept:'application/json'},cache:'no-store'}).then(r=>r.json()),fetch('/api/competition?season=2026',{headers:{Accept:'application/json'},cache:'no-store'}).then(r=>r.json())]);if(stats.error)throw new Error(stats.error);gamesPayload=stats;competitionPayload=comp;if(!Array.isArray(stats.liveGames)||!stats.liveGames.length)stats.liveGames=await WGameCards.fetchLiveGames();if(initial&&gamesMode==='live'&&!stats.liveGames.length)gamesMode='upcoming';WGameCards.populateFilter(document.getElementById('gamesTeamFilter'),stats);renderGames();status.textContent=stats.liveGames.length?`${stats.liveGames.length} live now · refreshes automatically · Eastern Time`:'Schedule connected · no regular-season games live right now · Eastern Time';if(initial)WGameCards.loadArtwork().then(renderGames);}catch(error){if(!gamesPayload){document.getElementById('gamesList').innerHTML='<div class="schedule-empty"><strong>Game schedule unavailable.</strong><p>Try again shortly.</p></div>';status.textContent='Schedule feed temporarily unavailable';}}finally{gamesRefreshActive=false;}}
loadGames(true);setInterval(()=>{if(!document.hidden)loadGames(false);},30000);
