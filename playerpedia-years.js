(()=>{let years=new Map();const key=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]/g,'');function decorate(){document.querySelectorAll('#playerGrid .player-card').forEach(card=>{const existing=card.querySelector('.player-card-years');const name=card.querySelector('strong')?.textContent?.trim();if(!name)return;const item=years.get(key(name));const label=item?.startYear?`${item.startYear}–`:'';if(!label){existing?.remove();return;}const badge=existing||document.createElement('small');badge.className='player-card-years';badge.textContent=label;if(!existing)card.querySelector('.player-card-copy')?.appendChild(badge);});}fetch('/api/player-years?season=2026',{headers:{Accept:'application/json'}}).then(r=>r.json()).then(p=>{years=new Map((p.players||[]).map(x=>[key(x.name),x]));decorate();}).catch(()=>{});const host=document.getElementById('playerGrid');if(host)new MutationObserver(decorate).observe(host,{childList:true});setTimeout(decorate,800);})();
